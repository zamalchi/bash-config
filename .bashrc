# .bashrc

################
################
### OBSOLETE ###
################
################

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi


if [ -f ~/.bash_config ]; then
	. ~/.bash_config
fi


if [ -f ~/.bash_local ]; then
	. ~/.bash_local
fi
